module.exports = {
    apps: [
        <% node['pm2']['apps'].each do |app| %>
        {
            name: '<%= app['name'] %>',
            script: '<%= app['script'] %>',
            interpreter: '<%= app['interpreter'] %>'
        },
        <% end %>
    ]
}