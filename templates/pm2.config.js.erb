module.exports = {
    apps: [
        <% @apps.each do |app| %>
        {
            name: '<%= app['name'] %>',
            script: '<%= app['script'] %>',
            interpreter: '<%= app['interpreter'] %>'
        },
        <% end %>
    ]
}