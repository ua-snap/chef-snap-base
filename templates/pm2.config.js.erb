module.exports = {
    apps: [
        <% @apps.each do |app| %>
        {
            name: '<%= app['name'] %>',
            script: '<%= app['script'] %>',
            interpreter: '<%= app['interpreter'] %>'
        },
        <% end %>
    ],
    hooks: {
        pre_start: {
            cmd: 'prefect config set PREFECT_API_URL=https://prefect.earthmaps.io/api',
            blocking: true
        }
    }
}